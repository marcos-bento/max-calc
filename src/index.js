import React from 'react';
import ReactDOM from 'react-dom/client';
import PaginaMatematica from './pages/paginaMatematica/paginaMatematica.tsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PaginaMatematica />
  </React.StrictMode>
);
